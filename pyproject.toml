[project]
name = "agent-observe"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.100.0",
    "opentelemetry-proto>=1.21.0",
    "protobuf>=4.25.0",
]

[tool.uv]
package = false
